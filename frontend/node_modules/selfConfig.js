const theUrl = "http://192.168.0.104:5000";

const getAdminList = theUrl+'/getAdminList';

const tokenHeaders = (token) => {
    return {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        'Authorization': "Bearer " + token
    }
} 

export {
    theUrl,
    getAdminList,
    tokenHeaders
}